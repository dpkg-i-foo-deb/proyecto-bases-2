--Consulta usando BETWEEN

--Enunciado: Listar las solicitudes de productos aprobadas entre el día 10 y 12 de Octubre del año 2022

SELECT s.ID_SOLICITUD, s.FECHA, s.CEDULA_PERSONA, s.CEDULA_EMPLEADO, s.ID_PRODUCTO, s.ACEPTADA
FROM SOLICITUD s
JOIN PRODUCTO p 
ON s.ID_PRODUCTO = p.ID_PRODUCTO
WHERE s.ACEPTADA = 'Y'
		AND s.FECHA  BETWEEN 
		TO_DATE ('2022-09-10', 'YYYY-MM-DD')
		AND
		TO_DATE ('2022-09-13', 'YYYY-MM-DD')
GROUP BY s.ID_SOLICITUD, s.FECHA, s.CEDULA_PERSONA, s.CEDULA_EMPLEADO, s.ID_PRODUCTO, s.ACEPTADA 
ORDER BY 1;
